## This is a normal heading

The above should pass, because it is an ATX (hash-mark) heading and not a setext heading.

The below line should fail.

Level 2 heading
===============

As well as the following line:

Level 2 heading
----

But dashes by themselves on a line is fine, because that's a horizontal rule.

----
