## Emphasis

This has *real asterisk* emphasis, so asterisk becomes the expected style.

An inline `_underscore_` span must not count as a mismatch.

<!-- _underscore_ inside an HTML comment must not count -->

<div>
_underscore_ inside an HTML block must not count
</div>
